Basic shell script for creating diary entries.

Execute with ./diary.sh. New entries are created in the directory stored in ${entries_dir}.

A new entry is created with the date as the filename. If that filename already exists, a version number is incremented and added to the end of the filename.
